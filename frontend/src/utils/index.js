/**
 * DarkTrust – Utils barrel export
 *
 * Frontend utility functions for common operations.
 * Add exports here as utilities are developed.
 *
 * Examples:
 * export { formatDate, formatRelativeTime } from './dateUtils';
 * export { formatBytes, formatNumber }      from './formatUtils';
 * export { cn }                             from './classNames';
 * export { truncate, capitalize }           from './stringUtils';
 * export { getTrustScoreColor,
 *           getTrustScoreLabel }            from './trustUtils';
 * export { parseApiError }                  from './errorUtils';
 * export { debounce, throttle }             from './functionUtils';
 */
