import React from 'react';
/**
 * Placeholder LineChart component.
 * In a real app this would wrap a chart library (e.g., Chart.js or recharts).
 */
export default function LineChart({ data }) {
  return (
    <div className="w-full h-64 flex items-center justify-center bg-surface-800/30 rounded-lg">
      <span className="text-surface-400">LineChart placeholder</span>
    </div>
  );
}
