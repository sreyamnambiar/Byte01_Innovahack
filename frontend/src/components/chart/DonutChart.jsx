import React from 'react';
/**
 * DonutChart – placeholder component for threat distribution.
 * In a real implementation this would wrap a chart library.
 */
export default function DonutChart({ data = [] }) {
  return (
    <div className="w-full h-64 flex items-center justify-center bg-surface-800/30 rounded-lg">
      <span className="text-surface-400">DonutChart placeholder</span>
    </div>
  );
}
