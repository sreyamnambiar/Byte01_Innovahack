/**
 * DarkTrust – Context barrel export
 *
 * Global React Context providers for state management.
 * Add exports here as contexts are developed.
 *
 * Examples:
 * export { AuthProvider, useAuth }             from './AuthContext';
 * export { ThemeProvider, useTheme }           from './ThemeContext';
 * export { NotificationProvider, useNotification } from './NotificationContext';
 * export { TrustContextProvider, useTrust }    from './TrustContext';
 */
